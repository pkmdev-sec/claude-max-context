# Contributing

1. Fork the repo
2. Make changes
3. Run `bash tests/run-all.sh` — all 47 must pass
4. If you change benchmark numbers, explain the math
5. Don't claim things we can't prove — see `lib/benchmark.sh` for the standard
6. PR with a clear description

That's it.
